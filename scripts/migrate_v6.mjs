import postgres from 'postgres'; async function migrate() { const oldSql = postgres('postgresql://neondb_owner:npg_X6Psxz4BVkLi@ep-dawn-mouse-ad04bowk-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require'); const newSql = postgres('postgres://postgres.nubnfwfkbivbevfmmjfc:hh1zp6U93Na5DXpK@aws-1-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require'); try { console.log('Fetching data...'); const users = await oldSqlSELECT * FROM users; for (const user of users) { await newSqlINSERT INTO users (anilist_id, username, avatar_url, created_at) VALUES (, , , ) ON CONFLICT (anilist_id) DO UPDATE SET username = EXCLUDED.username, avatar_url = EXCLUDED.avatar_url; } const newUserMap = new Map(); const res = await newSqlSELECT id, anilist_id FROM users; res.forEach(u => newUserMap.set(u.anilist_id, u.id)); const ratings = await oldSqlSELECT * FROM player_ratings; for (const r of ratings) { const orig = users.find(u => u.id === r.user_id); if (!orig) continue; const nId = newUserMap.get(orig.anilist_id); await newSqlINSERT INTO player_ratings (user_id, game_type, rating, games_played, wins, best_streak, current_streak, last_played, created_at, updated_at) VALUES (, , , , , , , , , ) ON CONFLICT DO NOTHING; } const sessions = await oldSqlSELECT * FROM game_sessions; for (const s of sessions) { const orig = users.find(u => u.id === s.user_id); if (!orig) continue; const nId = newUserMap.get(orig.anilist_id); await newSqlINSERT INTO game_sessions (user_id, game_type, score, max_score, questions_count, correct_count, difficulty, rating_change, created_at) VALUES (, , , , , , , , ) ON CONFLICT DO NOTHING; } console.log('Migration done'); } catch (e) { console.error(e); } finally { await oldSql.end(); await newSql.end(); } } migrate();